package cz.cvut.omo.sp;

/**
 * The `Entity` interface is a marker interface that signifies an entity within the system.
 * It serves as a common base interface for various types of entities in the application.
 * Implementing classes are expected to provide specific behavior and attributes for each entity.
 */
public interface Entity {
}
